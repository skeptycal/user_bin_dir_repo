#!/usr/bin/env sh

# pip keeps getting messes up while using macOS Big Sur Beta ...
# I can't figure out why ... it claims to be missing an internal import
# so I reinstall it as needed ...

~/bin/get-pip.py &
